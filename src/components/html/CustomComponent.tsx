import React from 'react'
import { CustomButton } from './Button'

export const CustomComponent = (props: React.ComponentProps<typeof CustomButton>) => {
  return (
    <div>
        {/* 
            Gets all props of a custom button component
        */}
    </div>
  )
}
